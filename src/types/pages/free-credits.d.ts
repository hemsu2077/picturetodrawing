export interface FreeCreditsPage {
  meta: {
    title: string;
    description: string;
    robots: {
      index: boolean;
      follow: boolean;
    };
  };
  hero: {
    title: string;
    description: string;
  };
}
